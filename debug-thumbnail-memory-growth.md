# [OPEN] 缩略图内存持续增长

## 现象

Windows 运行 27,457 条目时，主进程工作集从约 151MB 增长到约 3.2GiB。

## 证据

- `entries=27457` 始终不变。
- `thumbnails=3808 -> 8144 -> 12528 -> 16056` 持续增加。
- `thumbnail_loaded` 与 `thumbnails` 同步增加。
- `thumbnail_pending=8` 长时间不变。
- `open_html_bytes` 仅为几 KB，WebView2 子进程约 150MB。

## 假设

1. 缩略图调度状态没有正确阻止重复下载。
2. UI 纹理缓存持续持有已经不需要的纹理。
3. 任务结果通道或 pending 状态没有正常收敛。
4. 同一图片 URL 被多个条目重复创建纹理，放大了泄漏。

## 状态

已获得运行时证据，正在基于调度和纹理生命周期代码实施最小修复。
