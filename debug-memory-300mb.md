# [OPEN] 内存占用接近 300MB 调试记录

## 症状

- 用户反馈：应用内存占用接近 300MB。
- 当前环境：Windows，任务管理器进程组截图。

## 可验证假设

1. WebView2 单实例及其渲染进程是主要占用来源，主进程本身未明显增长。
2. 缩略图/图片缓存把大量图片数据和纹理同时保留在内存中。
3. 列表或条目投影在刷新/滚动后持续累积，导致应用进程内存增长。
4. 多次打开文章或 HTML 重渲染造成 WebView 文档/资源未及时释放。
5. 任务管理器显示的是应用总内存，包含 WebView2 子进程，Glean 主进程实际占用低于 300MB。

## 证据

用户提供的 Windows 任务管理器截图：

- 进程组 `glean-spike.exe (7)`：306.0 MB
- Glean 主进程：171.3 MB
- 6 个 WebView2 子进程：48.5 + 60.9 + 3.3 + 5.9 + 1.7 + 14.5 = 134.8 MB
- 主进程约占总量 56%，WebView2 子进程约占总量 44%。

初步判定：

- 假设 5「300MB 只是 WebView2 子进程合计」不成立。
- 假设 1「WebView2 是主要来源」不成立；WebView2 占比较大，但不是主因。
- 假设 2、3、4 仍未区分，需要记录 Glean 主进程中的列表、缩略图、图片缓存和阅读区状态。

## 状态

- [x] 添加运行时观测（每 10 秒写入 `glean-debug.log`）
- [x] 收集第一轮进程拆分数据
- [ ] 确认主进程根因
- [ ] 实施最小修复
- [ ] 对比修复后数据
