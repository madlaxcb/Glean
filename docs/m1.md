# M1 — 订阅垂直切片

> 状态：**已落地** · 2026-07-28

## 范围

| 项 | 状态 |
|----|------|
| `reqwest` (rustls, blocking) 拉 feed | Done |
| ETag / Last-Modified 条件请求 | Done |
| `feed-rs` 解析 RSS/Atom | Done |
| `ammonia` 消毒 + **默认去掉远程 img** | Done |
| `AddFeedFromUrl` / `RefreshFeeds` | Done |
| UI：URL 输入 + 添加 + 刷新 | Done |
| 异步刷新 | Done（`prepare_refresh_tasks` / `apply_refresh_outcome`；UI 走 prepare→worker thread→apply） |
| 持久化 `%APPDATA%` | Done（`paths::data_base_dir`：`%APPDATA%\Glean` / `~/.local/share/Glean` / 便携模式 `./data`） |

## 用法

1. 顶栏下方粘贴 feed URL → **添加订阅**
2. **刷新** 更新全部源
3. 列表/已读仍走 Command→Event

## 测试

```bat
cargo test -p glean-core
```

离线：解析样例 XML + upsert 去重。在线抓取需本机网络。
