# M0 UI Spike 记录

> 状态：**进行中（部分观察已通过，门禁未最终签字）**  
> 产物：GitHub Actions artifact `glean-spike-windows-x64`  
> 对应方案：[Glean-开发方案.md](./Glean-开发方案.md) §9.0  
> 路径：**A — egui + WebView2**（用户已选定）

## 环境

| 项 | 值 |
|----|-----|
| 测试机 OS | Windows（用户本机，版本待填） |
| DPI 缩放 | （100% / 125% / 150% — 待填） |
| 多显示器 | 有 / 无 — 待填 |
| WebView2 | 已能渲染样例 HTML（版本号待填） |
| 构建来源 | Actions artifact `glean-spike-windows-x64` |
| 日期 | 2026-07-28 起 |

## 宿主模式

| 模式 | 选用 | 备注 |
|------|------|------|
| H1 Child embed | ☑ 实测可用 | child WebView + set_bounds 跟踪阅读区 |
| H2 Follow overlay | ⚠ 按钮可切换 | **实现上仍与 H1 相同**（child bounds）；真顶层跟随窗未做 |

**选定（暂定）：** H1 Child embed（在 H2 真实现前）

## 已从截图 / 运行观察到的结果

| 观察 | 结果 |
|------|------|
| WebView 单实例加载静态 HTML | 通过（Article 1/3 正文正常） |
| 壳层中文（导航/列表） | 通过（加载系统 CJK 字体后） |
| 三栏背景无大块黑底 | 通过 |
| Stress 换文（opens 计数上升） | 通过（曾见 opens=161） |
| 搜索框可输入（含 IME 提示） | 部分（见框内文字，正式 IME 仍需勾表） |

## §9.0.2 量化表

| # | 场景 | Pass | Fail 现象 | 备注 |
|---|------|------|-----------|------|
| 1 | 分隔条 / 改窗口大小 | ☐ | | 对齐误差 ≤2px — **请实测** |
| 2 | 最大化 / 还原 / 最小化恢复 | ☐ | | **请实测** |
| 3 | 100% / 125% / 150% DPI | ☐ | | **请实测** |
| 4 | 跨 DPI 显示器拖移 | ☐ | | 可 N/A |
| 5 | 壳搜索框中文 IME | ☐ | | 候选框位置是否正常 |
| 6 | 列表 ↔ 阅读区焦点 | ☐ | | |
| 7 | 文内 https 链接 | ☐ | | 点 Article 1 外链，应系统浏览器打开 |
| 8 | Stress ×50 内存 | ☐ | | 任务管理器看 Private Bytes，无线性暴涨 |
| 9 | H2 跟手性 | N/A | | 与 H1 同实现前不单独判 Pass |

## 门禁结论

- [ ] **Pass** — 关键项 1–8 全过，可进入 M0b（workspace/SQLite/CI 业务骨架）
- [ ] **Fail** — 切换路径 B（Tauri 2），原因：

> 当前**不能**因「能打开 exe + 能看文章」就判 Pass。  
> 关键剩余项是 resize/DPI/IME/焦点/链接/内存。

## 已知实现债（不阻塞勾表，但要诚实）

1. H1/H2 代码路径相同；H2 真顶层 overlay 未做。  
2. 早期用窗口标题 `FindWindow`；已改为 **按本进程 PID EnumWindows** 取主窗。  
3. `request_repaint` 每帧刷新，Spike 可接受；产品期应收紧。  
4. 阅读区默认关远程图 / 关 JS 的产品策略尚未接到 wry 开关（HTML 已无 script）。

## 录屏 / 截图

- 用户提供：CJK 修复前 tofu + 黑底；修复后三栏正常 + Article 3。

## 决策签名

测试人：________  日期：________

### 快速自测顺序（5 分钟）

1. 拖左右分隔条，看 WebView 是否贴齐阅读区  
2. 最大化 → 还原 → 最小化再开  
3. 搜索框输入中文（拼音 IME）  
4. 打开 Article 1，点蓝色外链  
5. 点 **Stress x50**，任务管理器看内存是否只小幅波动  
6. 把上表 1/2/5/7/8 勾上，回填本文件或直接告诉开发者结果  
